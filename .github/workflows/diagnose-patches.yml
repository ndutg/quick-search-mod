name: Diagnose Patch Conflicts

on:
  workflow_dispatch:

jobs:
  diagnose:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout restore-wallpaper-feature
        uses: actions/checkout@v4
        with:
          ref: restore-wallpaper-feature

      - name: Show current WallpaperUtils.kt contrast injections
        run: |
          echo "=========================================="
          echo "=== WallpaperUtils.kt — our injections ==="
          echo "=========================================="
          grep -n -B 5 -A 5 "WallpaperContrastUtils" app/src/main/java/com/tk/quicksearch/shared/util/WallpaperUtils.kt

      - name: Show LocalWallpaperIsLight context in Theme.kt
        run: |
          echo "=========================================="
          echo "=== Theme.kt — LocalWallpaperIsLight ==="
          echo "=========================================="
          grep -n -B 3 -A 10 "LocalWallpaperIsLight" app/src/main/java/com/tk/quicksearch/shared/ui/theme/Theme.kt

      - name: Show AppGridView.kt label patch context
        run: |
          echo "=========================================="
          echo "=== AppGridView.kt — white label fix ==="
          echo "===========================
